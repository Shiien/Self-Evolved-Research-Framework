# Idea Backlog

> Off-scope ideas parked for later — NOT evidence, NOT a task. See
> `CLAUDE.md § Hypothesis Closure & Scope Discipline`.
>
> Format: `- [ ] {idea} — relates to: {future question it might answer} —
> why not now: {reason} — revisit when: {condition}`
>
> Check off (`- [x]`) and link the resulting idea/experiment when picked up.
> Never delete — the record of what was deliberately deferred is itself
> useful.

(empty — nothing parked yet)
